import Foundation
import CoreGraphics

enum AppConstants {
    enum Popover {
        static let expandedWidth: CGFloat = 500
        static let expandedHeight: CGFloat = 800
        static let minimizedWidth: CGFloat = 300
        static let minimizedHeight: CGFloat = 600
    }

    enum CenteredMenu {
        static let width: CGFloat = 580
        static let height: CGFloat = 380
        static let maxRecentItems: Int = 3
    }

    enum SettingsWindow {
        static let width: CGFloat = 480
        static let height: CGFloat = 560
    }
}
