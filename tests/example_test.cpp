#include <gtest/gtest.h>

// Basic test to verify test infrastructure is working
TEST(ExampleTest, BasicAssertion) {
    EXPECT_EQ(1, 1);
}

// Additional test to verify multiple tests work
TEST(ExampleTest, TrueAssertion) {
    EXPECT_TRUE(true);
}
